import { SubgraphNamePipe } from './subgraph-name.pipe';

describe('SubgraphNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SubgraphNamePipe();
    expect(pipe).toBeTruthy();
  });
});
